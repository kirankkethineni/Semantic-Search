# Semantic-Search
Instead of data-hungry, class-specific CNNs, we use a knowledge-driven approach: CNNs extract semantic leaf-disease features and query a curated database of disease descriptions. New diseases are added via database updates, avoiding large labeled datasets and model retraining.
